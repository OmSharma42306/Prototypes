# Prototypes

This repository contains small, focused experiments to understand distributed systems through hands-on implementation.

Each folder answers one specific question. Only the absolute bare minimum is implemented to observe behavior and build intuition.

The goal is understanding, not building reusable projects.
